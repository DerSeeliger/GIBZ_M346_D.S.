# SSH key pair — generated by Terraform, private key saved to m346-key.pem
resource "tls_private_key" "main" {
  algorithm = "RSA"
  rsa_bits  = 4096
}

resource "aws_key_pair" "main" {
  key_name   = "${var.project_name}-key-${var.student_name}"
  public_key = tls_private_key.main.public_key_openssh
  tags       = { Name = "${var.project_name}-keypair", Owner = var.student_name }
}

resource "local_sensitive_file" "private_key" {
  content         = tls_private_key.main.private_key_pem
  filename        = "${path.module}/m346-key.pem"
  file_permission = "0600"
}

# E-1-B: Single web server EC2 — public IP, Nginx, t2.micro (free tier)
resource "aws_instance" "web" {
  ami                         = data.aws_ami.amazon_linux_2023.id
  instance_type               = "t2.micro"
  subnet_id                   = data.aws_subnets.default.ids[0]
  vpc_security_group_ids      = [aws_security_group.web.id]
  key_name                    = aws_key_pair.main.key_name
  associate_public_ip_address = true
  user_data                   = base64encode(file("${path.module}/userdata-web.sh"))

  tags = {
    Name   = "${var.project_name}-web-${var.student_name}"
    Owner  = var.student_name
    Backup = "true"
  }
}
